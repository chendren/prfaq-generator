# Server-side key (set this in Vercel dashboard — NEVER commit this file with a real key)
# If set, users do not need to enter their own key.
# If not set, users are prompted to enter their own Anthropic API key.
ANTHROPIC_API_KEY=
